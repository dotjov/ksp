{
  "name": "ksp",
  "version": "1.1.3",
  "description": "A simple framework for build your app more easiest !",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "nyc --reporter=html --reporter=text mocha",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "report-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "repository": "github:dotjov/ksp-typescript",
  "keywords": [
    "ksp",
    "easy",
    "build",
    "app"
  ],
  "author": "dotjov <dotjov@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/debug": "^4.1.2",
    "@types/node": "^11.10.4",
    "assert": "^1.4.1",
    "codecov": "^3.2.0",
    "coveralls": "^3.0.3",
    "mocha": "^6.0.2",
    "nyc": "^13.3.0",
    "typescript": "^3.3.3333"
  },
  "dependencies": {
    "debug": "^4.1.1"
  }
}